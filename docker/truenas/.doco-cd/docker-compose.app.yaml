---
# This file is not managed by doco-cd because it can't manage itself
# Instead it is deployed through the TrueNAS SCALE UI

configs:
  poll-config.yml:
    content: |
      - run_once: true
        url: https://github.com/GiorgioAresu/home-ops.git
secrets:
  1pw_token:
    file: /mnt/tank/homes/admin/.config/doco-cd/1pw_token
  webhook_secret:
    file: /mnt/tank/homes/admin/.config/doco-cd/webhook_secret
services:
  app:
    configs:
      - source: poll-config.yml
        target: /poll-config.yml
    container_name: doco-cd
    environment:
      HTTP_PORT: 8880
      LOG_LEVEL: info
      POLL_CONFIG_FILE: /poll-config.yml
      SECRET_PROVIDER: 1password
      SECRET_PROVIDER_ACCESS_TOKEN_FILE: /run/secrets/1pw_token
      WEBHOOK_SECRET_FILE: /run/secrets/webhook_secret
      DEPLOY_CONFIG_BASE_DIR: ./docker/truenas/
    healthcheck:
      test:
        - CMD
        - /doco-cd
        - healthcheck
      start_period: 15s
      interval: 30s
      timeout: 5s
      retries: 3
    image: ghcr.io/kimdre/doco-cd:0.92.0@sha256:d38be697f3191c257a8ddeb2235bf2dc95f5378f7ba76fa4060d25fdd1dc8320
    ports:
      - 8880:8880
    restart: unless-stopped
    secrets:
      - 1pw_token
      - webhook_secret
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - data:/data
volumes:
  data: {}
