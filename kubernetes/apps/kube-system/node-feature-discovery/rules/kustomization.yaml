---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./apc-ups-device.yaml
  # - ./intel-gpu-device.yaml # TODO: check if needed with intel-device-plugin
  - ./phoscon-zigbee-device.yaml
